import { GetidstringPipe } from './getidstring.pipe';

describe('GetidstringPipe', () => {
  it('create an instance', () => {
    const pipe = new GetidstringPipe();
    expect(pipe).toBeTruthy();
  });
});
